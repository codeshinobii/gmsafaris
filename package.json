{
  "private": true,
  "scripts": {
    "build:purge": "purgecss --config purgecss.config.js --output css/purged",
    "build:critical": "node generate-critical-css.mjs",
    "build": "npm run build:purge && npm run build:critical",
    "build:all": "npm run build && echo '✓ PurgeCSS + critical CSS generated. Run `git add css/purged/` to deploy.'"
  },
  "devDependencies": {
    "bootstrap": "^5.0.0",
    "critical": "^5.0.0",
    "purgecss": "^5.0.0",
    "sass": "^1.99.0"
  }
}
